name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    # using Ubuntu
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1 #this installs node and npm for us
      with:
        node-version: '12.x'

    - uses: actions/cache@v1 # this allows for re-using node_modules caching, making builds a bit faster.
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-

    - run: npm install
    - run: npm install -g @angular/cli > /dev/null
    - run: ng build --prod
    
    - name: Delete Existing Files
      run : sudo sshpass -p ${{ secrets.PASSWORD }} -v ssh -o StrictHostKeyChecking=no ayan@198.61.147.136 'rm -rf /home/ayan/contractors.crmrunner.com/*'

    - name: copy file via ssh key
      uses: ./
      with:
        host: 198.61.147.136
        username: ayan
        password: ${{ secrets.PASSWORD }}
        source: "./dist/affiliate/"
        target: "/home/ayan/contractors.crmrunner.com/"
        strip_components: 3 # this is important
